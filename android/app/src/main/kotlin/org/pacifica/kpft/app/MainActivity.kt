package org.pacifica.kpft.app

// This app launches via AudioServiceActivity (see AndroidManifest.xml).
// Keep a no-op MainActivity to satisfy any tooling that expects it,
// but avoid depending on Flutter embedding here.
class MainActivity
